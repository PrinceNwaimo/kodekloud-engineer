Problem
Install ansible version 4.8.0 on Jump host using pip3 only. Make sure Ansible binary is available globally.

Solution
bash
# Update pip3
sudo pip3 install --upgrade pip

# Install Ansible 4.8.0
sudo pip3 install ansible==4.8.0

# Verify installation
ansible --version
