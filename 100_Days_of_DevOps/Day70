The Nautilus team is integrating Jenkins into their CI/CD pipelines. After setting up a new Jenkins server, they're now configuring user access for the development team, Follow these steps:
1. Click on the Jenkins button on the top bar to access the Jenkins UI. Login with username admin and password Adm!n321.
2. Create a jenkins user named rose with the password ksH85UJjhb. Their full name should match Rose.
3. Utilize the Project-based Matrix Authorization Strategy to assign overall read permission to the rose user.
4. Remove all permissions for Anonymous users (if any) ensuring that the admin user retains overall Administer permissions.
5. For the existing job, grant rose user only read permissions, disregarding other permissions such as Agent, SCM etc.

Note:

1. You may need to install plugins and restart Jenkins service. After plugins installation, select Restart Jenkins when installation is complete and no jobs are running on plugin installation/update page.
2. After restarting the Jenkins service, wait for the Jenkins login page to reappear before proceeding. Avoid clicking Finish immediately after restarting the service.
3. Capture screenshots of your configuration for review purposes. Consider using screen recording software like loom.com for documentation and sharing.

SOLUTION
Here's exactly how to complete the task in Jenkins.

1. Log in to Jenkins

Open Jenkins.

Username: admin
Password: 
2. Install the Matrix Authorization plugin (if not installed)

Go to:

Manage Jenkins → Plugins

Under Available plugins, search for:

Matrix Authorization Strategy Plugin

Install it.

When prompted:

✅ Check Restart Jenkins when installation is complete and no jobs are running

Wait until Jenkins completely restarts and the login page appears again.

Log in again.

3. Create the user

Go to:

Manage Jenkins → Security → Users

(or Manage Jenkins → Manage Users, depending on Jenkins version)

Click Create User.

Enter:

Username: rose
Password:
Confirm password: 
Full name: Rose
Email: (can be left blank if not required)

Click Create User.

4. Enable Project-based Matrix Authorization

Go to

Manage Jenkins → Security

Under Authorization, select:

Project-based Matrix Authorization Strategy

If you don't see it, the plugin wasn't installed correctly.

5. Configure Overall permissions

Add the users.

Add
admin
rose
Admin permissions

Give admin:

✅ Overall → Administer

(Everything else is automatically allowed.)

Rose permissions

Only enable:

✅ Overall → Read

Nothing else.

6. Remove Anonymous permissions

Find the row:

anonymous

Either:

remove the user entirely, or
uncheck every permission.

Ensure anonymous has no permissions.

7. Configure Job permissions

Open the existing job.

Click

Configure

Near the bottom you'll see:

Enable project-based security

(or Project-based Matrix Authorization)

Enable it.

Add user:

rose

Grant only:

✅ Read

Leave everything else unchecked:

Build ❌
Configure ❌
Delete ❌
Discover ❌
Workspace ❌
Cancel ❌
SCM ❌
Agent ❌
Extended Read ❌

Save.

8. Verify

Log out.

Login as:

Username: rose
Password: 

You should be able to:

View Jenkins
Open the existing job

You should not be able to:

Build
Configure
Delete
Create jobs
Manage Jenkins
9. Take screenshots

Capture screenshots of:

The rose user in Manage Users.
Manage Jenkins → Security showing:
Project-based Matrix Authorization enabled.
admin with Administer.
rose with only Overall → Read.

This task is about implementing Role-Based Access Control (RBAC) in Jenkins using the Project-based Matrix Authorization Strategy. The goal is to ensure that users have only the permissions they need, following the principle of least privilege.
anonymous with no permissions.
The existing job's Configure page showing rose has only Read permission.

These screenshots satisfy the documentation requirement.
